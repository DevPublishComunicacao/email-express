services:
  postgres:
    image: postgres:16
    container_name: email-express-db
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: email_express
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
